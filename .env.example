# UGV Rover PT - Docker Environment
# ----------------------------------
# Minimal environment for Docker Compose.
# Most configuration now lives in config/params.yaml

# ROS 2 domain (for multi-robot setups)
ROS_DOMAIN_ID=0

# Mission data storage path (mounted into container)
UGV_MISSION_ROOT=/data/missions
